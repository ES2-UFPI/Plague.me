<h1>Friends</h1>
<% @friends.each do |friend| %>
<ul>
  <li><%= link_to friend.nickname, user_path(friend) %></li>
</ul>
<% end %>

<%= link_to "Back", user_path(@user), class: 'btn btn-primary' %>