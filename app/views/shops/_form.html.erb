<%= form_with(model: shop) do |form| %>
  <% if shop.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shop.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
        <% shop.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :idshops, style: "display: block" %>
    <%= form.number_field :idshops %>
  </div>

  <div>
    <%= form.label :title_shop, style: "display: block" %>
    <%= form.text_field :title_shop %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
